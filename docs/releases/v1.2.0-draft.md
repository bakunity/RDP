# Hermes RDP v1.2.0 — draft

Статус: **черновик стабилизационного релиза**. Этот файл ведётся по мере прохождения PR/acceptance и не является опубликованным release note до финального merge/tag.

## Уже подтверждено

- OpenSSH остаётся транспортом Windows-клиентов; FRP не возвращается в runtime-архитектуру.
- Windows 10 x64 + PowerShell x86: подтверждена необходимость `Sysnative` для native Microsoft OpenSSH; installer resolver и regression coverage добавлены.
- Повторный `Добавить ПК` на уже установленной Hermes-машине больше не выполняет destructive actions: existing-install guard срабатывает раньше остановки Task/SSH и backup flow.
- Desired access и queued command меняются атомарно в SQLite.
- Dashboard разделяет heartbeat агента, желаемый доступ, применённое состояние агента, SSH-транспорт и публичный RDP endpoint.
- Public endpoint truth перенесён на Linux-сервер: listener определяется по `/proc/net/tcp*`; Windows self-probe удалён.
- RDP-сессии разделены на Hermes и прямые LAN/VPN подключения.
- Live acceptance на MIPC:
  - прямой RDP: `Hermes=0`, `LAN/VPN=1` — PASS;
  - RDP через Hermes: `Hermes=1`, `LAN/VPN=0` — PASS.
- Пользовательские статусы dashboard русифицированы; CI #101 на соответствующем head — PASS.

## Найдено во время acceptance — исправить до релиза

### Performance regression Windows agent

На реальной Windows-машине измерено:

```text
Get-NetTCPConnection -State Established        ≈ 1059 ms
Get-NetTCPConnection -LocalPort 3389           ≈ 516 ms
Get-CimInstance Win32_Process                  ≈ 357 ms
Get-TopProcesses sampling window               = 800 ms минимум
```

Текущий 3-секундный telemetry loop выполняет слишком много тяжёлых операций и может занимать большую часть интервала, что совпадает с наблюдаемыми микрофризами RDP.

План исправления:

- 3 сек: heartbeat/commands, access, SSH, RDP-channel state;
- 15 сек: CPU/RAM/disk/network/sessions/route/uptime;
- process list: только по явному режиму наблюдения;
- `Наблюдать 60с`: dashboard/resources обновляются каждые 3 сек, TOP processes реже, после 60 сек режим выключается автоматически;
- убрать full TCP table scan и повторные full `Win32_Process` scans из обычного 3-секундного цикла.

Статус: **CONFIRMED regression / PLANNED FIX**.

### Windows Server installer blocker

Installer содержит противоречивые проверки: `ProductType != 1` сразу запрещает Windows Server, хотя следующая проверка Caption включает `Server` как допустимый вариант.

Статус: **CONFIRMED BUG / PLANNED FIX**.

Нужно поддержать:

- Windows client: Pro / Enterprise / Education;
- Windows Server: server/member-server и domain-controller ProductType, если входящий RDP доступен;
- отдельный regression test и реальная fresh-install проверка на Windows Server.

## Следующий acceptance

1. оптимизировать telemetry loop и повторить timing benchmark на MIPC;
2. убедиться, что RDP микрофризы исчезли/заметно уменьшились;
3. внедрить 60-секундный on-demand monitoring UX и проверить auto-off;
4. исправить Windows Server gate и пройти реальную установку;
5. fresh install Win10 x64 из PowerShell x86 — финальный Sysnative installer acceptance;
6. после этого обновить durable `context/`, документацию и финальные release notes.

## Evidence rules

- **PASS / CONFIRMED** — есть реальный output/user confirmation;
- **IMPLEMENTED, NOT VALIDATED** — код есть, runtime acceptance ещё нет;
- **PLANNED** — только следующий шаг;
- не переносить hypothesis в release notes как готовый факт.
